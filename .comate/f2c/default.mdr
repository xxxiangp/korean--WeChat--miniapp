## 技术栈要求
请根据代码库分析项目使用的技术栈。

## 任务要求
1. 精确还原页面的视觉效果和布局尺寸
2. 遵循目标框架的最佳实践和代码规范，包括以下几点：

一：注意优化DOM结构与逻辑关系：
   - 在不影响前还原度的前提下优化DOM结构，避免不必要的div嵌套和冗余！
   - 优先使用HTML5语义化标签（nav, header, main, footer, article, section等）
   - 列表内容必须使用map等循环方法渲染
   - 确保所有可交互元素的可访问性

二：布局与样式规范：
   - 优先使用Flex或Grid布局，避免滥用绝对定位
   - 只在必要时使用绝对定位（如模态框、tooltip等）
   - 实现响应式设计，但要保证原尺寸的视觉效果

三：代码可读性与可维护性：
   - 组件名称和文件名应当语义化并使用PascalCase
   - 将页面拆分为合理的组件结构
   - 抽取重复逻辑
   - 为复杂逻辑添加简洁清晰的注释
   - 静态资源引用应当语义化命名

## 其他
